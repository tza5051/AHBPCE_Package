% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FOT_functionsValach.R
\name{compute_valach_r_lln}
\alias{compute_valach_r_lln}
\title{Calculate Lower Limit of Normal for Resistance (Valach/LEAD)}
\usage{
compute_valach_r_lln(
  sex,
  ht,
  ht_unit = "cm",
  age,
  weight,
  weight_unit = "kg",
  frequency,
  ...
)
}
\arguments{
\item{sex}{Character string: "Male" or "Female"}

\item{ht}{Numeric: Height value}

\item{ht_unit}{Character: "cm" (default) or "in" for height units}

\item{age}{Numeric: Age in years}

\item{weight}{Numeric: Weight value}

\item{weight_unit}{Character: "kg" (default) or "lbs" for weight units}

\item{frequency}{Character: Oscillation frequency - "5", "11", or "19" (Hz)}

\item{...}{Additional arguments (ignored)}
}
\value{
Numeric: LLN for resistance (hPa·s·L⁻¹)
}
\description{
Computes the lower limit of normal (5th percentile, z-score = -1.645) for respiratory
resistance using Valach et al. reference equations.
}
\details{
LLN calculated using GAMLSS Box-Cox transformation:
LLN = M × (-1.645 × S × L + 1)^(1/L)
where M = predicted median, S = coefficient of variation, L = Box-Cox parameter
}
\examples{
# Check if measured resistance is below LLN
lln <- compute_valach_r_lln(sex = "Male", ht = 180, age = 50, weight = 80, frequency = "5")
measured <- 0.25
is_below_lln <- measured < lln

}
